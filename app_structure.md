src/app/* = pages and app layout (App Router)
src/data/* = editable content/config data
src/components/* = reusable UI/layout components
src/types/* = shared TypeScript types
public/* = static assets (resume PDF)